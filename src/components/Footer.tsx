import React from 'react';

function Footer() {
  return (
    <footer>
      <p>© {new Date().getFullYear()} Pokédex App — Built with React + TypeScript</p>
    </footer>
  );
}

export default Footer;